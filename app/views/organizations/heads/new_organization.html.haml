= render 'validation_errors', :resource => @organization

= form_for @organization, url: create_organization_organizations_head_path do |f|
  = f.label :name
  = f.text_field :name
  %br
  = f.submit 'Create organization'
