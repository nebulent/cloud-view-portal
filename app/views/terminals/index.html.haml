= link_to "Add terminal", new_terminal_path

Terminals:
%table
  %tbody
    - @terminals.each do |t|
      %tr{:id => "terminal#{t.id}"}
        %td= t.name
        %td= t.uri
        %td= button_to "Delete", terminal_path(t), :method => :delete, 'data-confirm' => 'Are you sure?'

